# SAX v3.27.0 - Git Submodule 배포 방식 전환

> **Release Date**: 2025-11-27
> **Type**: MINOR (Breaking Change in Deployment Method)

## Summary

SAX 패키지 배포 방식이 **rsync/copy 기반**에서 **Git Submodule 기반**으로 전환되었습니다. 이로써 각 SAX 패키지가 독립적인 Git 레포지토리로 관리되며, 서비스 레포지토리에서 서브모듈로 설치하여 사용합니다.

## Added

### 독립 GitHub 레포지토리 생성

각 SAX 패키지가 별도의 GitHub 레포지토리로 분리되었습니다:

| 패키지 | GitHub Repository | 설명 |
|--------|------------------|------|
| sax-core | [semicolon-devteam/sax-core](https://github.com/semicolon-devteam/sax-core) | 공통 원칙 및 규칙 |
| sax-meta | [semicolon-devteam/sax-meta](https://github.com/semicolon-devteam/sax-meta) | SAX 패키지 관리 도구 |
| sax-po | [semicolon-devteam/sax-po](https://github.com/semicolon-devteam/sax-po) | PO/기획자용 |
| sax-next | [semicolon-devteam/sax-next](https://github.com/semicolon-devteam/sax-next) | Next.js 개발자용 |

### 형제 서브모듈 (Sibling Submodules) 방식 채택

- 중첩 서브모듈의 복잡성을 피하기 위해 **형제 서브모듈** 방식 채택
- sax-core와 도메인 패키지(sax-po, sax-next)가 `.claude/` 하위에 형제로 설치
- 각 도메인 패키지는 `@sax-core/` 참조를 통해 Core 문서 로드

### SAX 개발 워크스페이스 CLAUDE.md

- `/Users/jeonjunyeong/Desktop/Sources/semicolon/sax/.claude/CLAUDE.md` 생성
- SAX 패키지 개발 전용 워크스페이스 설정
- docs 레포와의 동기화 방향 명시

## Changed

### 배포 방식 변경

**Before (v3.26.x 이하)**:
```bash
# rsync 기반 복사
rsync -av --delete sax/packages/sax-po/ target-repo/.claude/sax-po/
```

**After (v3.27.0 이상)**:
```bash
# Git Submodule 기반 설치
git submodule add https://github.com/semicolon-devteam/sax-core.git .claude/sax-core
git submodule add https://github.com/semicolon-devteam/sax-po.git .claude/sax-po
```

### Source of Truth 유지

- docs 레포지토리(`semicolon-devteam/docs`)는 여전히 **Source of Truth**
- `docs/sax/core/` → sax-core 레포로 동기화
- `docs/sax/packages/sax-*` → 각 패키지 레포로 동기화

---

## Migration Guide

### 대상

v3.26.x 이하 버전의 SAX 패키지를 사용 중인 모든 서비스 레포지토리

### 사전 요구사항

- Git 2.13 이상 (서브모듈 개선 버전)
- GitHub CLI (`gh`) 설치 (권장)

### Step 1: 기존 SAX 파일 백업 및 제거

```bash
# 기존 .claude 디렉토리 백업
cp -r .claude .claude.backup

# 기존 SAX 관련 파일 제거 (CLAUDE.md는 유지)
rm -rf .claude/sax-core
rm -rf .claude/sax-po      # 또는 sax-next
rm -rf .claude/sax-meta    # 있는 경우

# 또는 전체 .claude 삭제 후 CLAUDE.md만 복원
rm -rf .claude
mkdir .claude
cp .claude.backup/CLAUDE.md .claude/  # 커스텀 설정이 있는 경우
```

### Step 2: Git Submodule로 설치

```bash
# 1. sax-core 설치 (필수)
git submodule add https://github.com/semicolon-devteam/sax-core.git .claude/sax-core

# 2. 도메인 패키지 설치 (하나 선택)

# PO/기획자용
git submodule add https://github.com/semicolon-devteam/sax-po.git .claude/sax-po

# 또는 Next.js 개발자용
git submodule add https://github.com/semicolon-devteam/sax-next.git .claude/sax-next
```

### Step 3: .gitmodules 확인

```bash
cat .gitmodules
```

예상 결과:
```ini
[submodule ".claude/sax-core"]
    path = .claude/sax-core
    url = https://github.com/semicolon-devteam/sax-core.git
[submodule ".claude/sax-po"]
    path = .claude/sax-po
    url = https://github.com/semicolon-devteam/sax-po.git
```

### Step 4: 커밋

```bash
git add .gitmodules .claude/
git commit -m "🔧 Migrate SAX to Git Submodule (v3.27.0)"
```

### Step 5: 팀원 동기화 안내

다른 팀원들이 레포를 클론하거나 풀할 때:

```bash
# 신규 클론 시
git clone --recurse-submodules <repo-url>

# 기존 레포 업데이트 시
git pull
git submodule update --init --recursive
```

### Step 6: 업데이트 방법 (향후)

SAX 패키지 업데이트가 있을 때:

```bash
# 모든 서브모듈 최신화
git submodule update --remote

# 변경사항 커밋
git add .claude/
git commit -m "🔧 Update SAX packages"
```

---

## Structure Comparison

### Before (v3.26.x)

```text
your-project/
├── .claude/
│   ├── CLAUDE.md           # 패키지 설정
│   ├── sax-core/           # rsync로 복사된 파일
│   │   ├── PRINCIPLES.md
│   │   └── ...
│   └── sax-po/             # rsync로 복사된 파일
│       ├── agents/
│       ├── skills/
│       └── ...
```

### After (v3.27.0)

```text
your-project/
├── .claude/
│   ├── CLAUDE.md           # 패키지 설정
│   ├── sax-core/           # Git Submodule (semicolon-devteam/sax-core)
│   │   ├── PRINCIPLES.md
│   │   └── ...
│   └── sax-po/             # Git Submodule (semicolon-devteam/sax-po)
│       ├── agents/
│       ├── skills/
│       └── ...
├── .gitmodules             # 서브모듈 설정 파일 (신규)
```

---

## Troubleshooting

### 서브모듈 클론 실패

```bash
# SSH 대신 HTTPS 사용
git config --global url."https://github.com/".insteadOf "git@github.com:"

# 또는 개별 서브모듈 재시도
git submodule update --init .claude/sax-core
```

### 서브모듈 충돌

```bash
# 서브모듈 상태 확인
git submodule status

# 강제 업데이트
git submodule update --force --remote
```

### 기존 디렉토리와 충돌

```bash
# 기존 디렉토리 완전 제거 후 재설치
rm -rf .claude/sax-core
git submodule add https://github.com/semicolon-devteam/sax-core.git .claude/sax-core
```

---

## Benefits

1. **버전 관리 개선**: 각 패키지의 버전을 Git 커밋으로 정확히 추적
2. **업데이트 간소화**: `git submodule update --remote`로 간편 업데이트
3. **독립적 개발**: 각 패키지를 독립적으로 개발하고 테스트 가능
4. **팀 협업 개선**: 모든 팀원이 동일한 버전의 SAX 패키지 사용 보장

---

## References

- [sax-core Repository](https://github.com/semicolon-devteam/sax-core)
- [sax-meta Repository](https://github.com/semicolon-devteam/sax-meta)
- [sax-po Repository](https://github.com/semicolon-devteam/sax-po)
- [sax-next Repository](https://github.com/semicolon-devteam/sax-next)
- [Git Submodules Documentation](https://git-scm.com/book/en/v2/Git-Tools-Submodules)
